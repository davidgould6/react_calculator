--Calculator--

  --Front End--
  [x] Create all buttons (Numbers, Operators, Clear)
  [x] Create Input field for expression
  [x] Create local state to hold inputs from users
  [x] Upon equals send expression to server (POST)
  [ ] Show answer in input field... (GET) --Stretch--
    [x] Show last 10 answers most recent first (decsending) (GET)
  
  --Backend--
  [x] Create & Set up server
  [x] Create Database (react_calculator)
    [x] Create table to hold expression Database
  [x] Calculate Expression received from POST
    [x] Send query to DB with expression Database